{
  "name": "@projekt-r/dynamic-components-test",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "test": "NODE_ENV=test playwright test",
    "test:ui": "NODE_ENV=test playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "24"
  }
}
